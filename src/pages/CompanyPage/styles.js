import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles((theme) => ({
    paragraph: { margin: '24px 0px' },
    display: 'flex',
    flexDirection: 'row',
}))

export default useStyles
